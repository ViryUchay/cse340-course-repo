<%- include('partials/header') %>

<h1><%= title %></h1>
<p class="page-intro">Service projects are organized into the following categories.</p>

<section class="card-grid">
  <%= category %> to <%= category.name %>
    <article class="card">
      <h2><%= category %></h2>
    </article>
  <% }) %>
</section>

<%- include('partials/footer') %>
